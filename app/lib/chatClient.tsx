import { StreamChat } from 'stream-chat';

const apiKey = 'd8fk9qgytcdt'; // 🔁 Replace with your actual Stream API key

const chatClient = StreamChat.getInstance(apiKey);

export default chatClient;